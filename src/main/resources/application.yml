server:
  port: 9001
  tomcat:
    connection-timeout: 2s
    keep-alive-timeout: 15s
    threads:
      max: 50
      min-spare: 5

spring:
  profiles:
    active: dev
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
      exclude: target/**